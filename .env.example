# Copy to .env for local dev only. Use Heroku Config Vars in prod.
DATABASE_URL=postgres://user:pass@localhost:5432/credits
JWT_SECRET=change-me-super-secret # HS256 for auth tokens
# RS256 keys for QR tokens (PEM). If omitted locally, the app auto-generates ephemeral keys at boot.
JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----
...
-----END PUBLIC KEY-----
CORS_ALLOWED_ORIGINS=http://localhost:5500,https://yourname.github.io
PORT=3000
NODE_ENV=development
